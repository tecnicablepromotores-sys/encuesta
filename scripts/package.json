{
  "name": "sync-promoters",
  "version": "1.0.0",
  "private": true,
  "description": "Sincroniza usuarios (rol=promotor) a promotores_publicos via GitHub Actions",
  "main": "sync-promoters.js",
  "scripts": {
    "sync": "node sync-promoters.js"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
